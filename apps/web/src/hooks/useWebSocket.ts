// Thin wrapper to comply with checklist naming: useWebSocket
// Internally delegates to useNotifications which manages WS + notifications.
export { useNotifications as useWebSocket } from './useNotifications';
